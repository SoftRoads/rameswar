pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
	withMaven(maven : 'Maven'){
		sh ' compile'
	}
                
            }
        }
        stage('Test') { 
            steps {
                withMaven(maven : 'Maven'){
		sh ' test'
            }
        }
        stage('Deploy') { 
            steps {
                withMaven(maven : 'Maven'){
		sh ' deploy'
	}
            }
        }
    }
}